# This file was auto-generated by the Firebase CLI
# https://github.com/firebase/firebase-tools

name: Deploy to Firebase Hosting on merge
'on':
  push:
    branches:
      - release

jobs:
  build_and_deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - run: npm i && npm run build
      - env:
          VITE_FIREBASE_API_KEY: ${{ secrets.KEYSTORE_BASE64 }}
          VITE_FIREBASE_AUTH_DOMAIN: ${{ secrets.VITE_FIREBASE_AUTH_DOMAIN }}
          VITE_FIREBASE_PROJECT_ID: ${{ secrets.VITE_FIREBASE_PROJECT_ID }}
          VITE_FIREBASE_STORAGE_BUCKET: ${{ secrets.VITE_FIREBASE_STORAGE_BUCKET }}
          VITE_FIREBASE_MESSAGE_SEND_ID: ${{ secrets.VITE_FIREBASE_MESSAGE_SEND_ID }}
          VITE_FIREBASE_APP_ID: ${{ secrets.VITE_FIREBASE_APP_ID }}
          VITE_MEASUREMENT_ID: ${{ secrets.VITE_MEASUREMENT_ID }}
          VITE_MESSAGE_ID: ${{ secrets.VITE_MESSAGE_ID }}
          VITE_BACKEND_URL: ${{ secrets.VITE_BACKEND_URL }}
      - run: echo "$VITE_FIREBASE_API_KEY"
      - uses: FirebaseExtended/action-hosting-deploy@v0
        with:
          repoToken: '${{ secrets.GITHUB_TOKEN }}'
          firebaseServiceAccount: '${{ secrets.FIREBASE_SERVICE_ACCOUNT_APP_TODO_47999 }}'
          channelId: live
          projectId: app-todo-47999
          dotenv_path: .env
      - run: echo "$VITE_FIREBASE_API_KEY"
